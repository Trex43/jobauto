# 🚀 Render Deployment Guide - JobAuto

## Status: Step 1/4 ✅ Environment Ready

### Completed Files
- `backend/.env.example` ✅

## Next: Step 2 - Render PostgreSQL (2 mins)

**Commands to run NOW:**

```bash
# 1. Copy template
cp backend/.env.example backend/.env

# 2. Edit backend/.env (add Render DB URL later)
code backend/.env
```

**Browser:** [render.com](https://render.com) → New → PostgreSQL → Hobby → Create

**Get DATABASE_URL** → Paste into `backend/.env` → `git add/commit/push`

---

## Full Steps Preview

**Step 2:** Render Postgres DB ✅ Get `DATABASE_URL`
**Step 3:** Backend Web Service  
**Step 4:** Frontend Static Site  
**Step 5:** Seed & Test

**Live in <10 mins!**

```
DATABASE_URL=postgresql://... ← COPY THIS FROM RENDER
```

